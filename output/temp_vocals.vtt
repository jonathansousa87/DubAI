WEBVTT

00:01.077 --> 00:09.245
Hello, everyone, and welcome to this lecture on using PostgreSQL with Python and using the library PsychoPG2.

00:09.717 --> 00:32.617
So I'm right now using the Jupyter notebook system in order to show you how all this syntax works. You can feel free to use any editor or IDE that you prefer for Python. If you've already experienced one of my other Python courses, you're probably already really familiar with the Jupyter or IPython notebook system. If you're not and want to find out more, you can go to www.jupyter.org

00:32.617 --> 00:52.377
So let's go ahead and start by just importing

00:52.934 --> 01:17.825
the library that we downloaded from the previous lecture. So again, go to the previous lecture for more notes on how to actually download everything. And for the dot PI file that has all the code that I'm going to execute here, or the notebook file that has again, all the code and some notes of what I'm going to execute here during the lecture. So to get started, like I was saying, we need to import psycho PG too. So psycho,

01:19.900 --> 01:38.142
Let me make sure I spell that right. Psycho PG2. So by convention, we just import it as PG2. And then the next thing you're going to do is connect to your database. And by convention, that's C O N N. And you're just going to say PG2

01:39.593 --> 01:54.983
And then call the connect method. And you're going to pass in a couple of arguments here. The first one is what database you're going to connect to. I'm going to go ahead and connect to the DVD rental database that we've been working with a lot. And then you have to define the user.

01:55.675 --> 02:15.470
So depending on how you actually set up your PG admin account, uh, you may have set up your own particular username. If you don't specify the username in this argument of the connect method, it'll automatically by default on a Windows computer, um, and I believe on a Mac as well, default to the user, um, profile name.

02:15.470 --> 02:35.315
But if you've just been following along with all the lectures, the default for pgadmin as far as the username is just postgres. So if you're having any trouble with your own username, uh, try just saying postgres. And if you set everything to default, um, it should be postgres. And then the last thing you have to input is your password.

02:36.614 --> 02:50.182
Now, I've already set up my password as the variable called secret up here. So if you want, you can just set up a variable called secret and put in your password. Obviously, my password is not the string. Your password goes here.

02:50.283 --> 03:12.153
So if you've done all this correctly, you should be able to run this line of code or put this line of code inside your .ipy file. And then you should have a connection there. So in this Jupyter notebook system, I'm basically just running each of these cells as a separate block of code. And that way I can run the cells and show you the output at the same time.

03:14.465 --> 03:40.807
So now that we've established the connection to PostgreSQL, the next step is to retrieve the cursor. And the cursor is just a control structure that enables traversal over the records in a database. So you can basically think of it as some sort of iterator or pointer for SQL data retrieval. And the way we're going to do that is we're going to go ahead and create a variable called cur. And then we're going to have a connection.

03:40.942 --> 04:03.115
object. So that was this, uh, C O N N. And then we call the cursor method off of it. And so now we have our cursor. And that was essentially the last thing we needed to do in order to execute SQL queries from Python. So using that cursor object, I can say, execute off of it.

04:04.364 --> 04:28.816
And then you basically, inside the execute method, you pass in a string of whatever you want to actually ask the database or query the database of. So we can say something like this, select asterisks from, let's go ahead and say from payments. So now this cursor has that execute statement.

04:30.301 --> 04:47.227
And this cursor then has a couple of methods available. It has fetchAll, fetchMany, and fetchOne. So what these do is they basically return rows of data. So if I just say fetchOne, it will return the first row of that data.

04:47.395 --> 05:09.873
So these are the numbers that correspond with that row of data from select everything from payment. I can also say fetch many and then actually pass in how many rows I want to fetch. So I can fetch, uh, 10 rows and you'll notice that it returns it as a list of tuple items, which is nice because then you can use tuple and packing later on.

05:10.025 --> 05:21.685
And also note that depending on the data type, it'll also specify, like, a date time data type if you had a timestamp information on it for that particular, uh, column in your database table.

05:23.272 --> 05:43.673
So those are really the main features you're going to be using with, uh, CyclePG2. And as far as, uh, using this all in Python, it's going to be kind of up to you how you use these, uh, main three or four commands in order to use the power of Python and use the automation capabilities of Python to query your database or put things into your database.

05:43.808 --> 06:12.277
So again, let's just go over what we've done so far. You had to import PsychoPG2 as PG2. Then you had to establish a connection. I set it as the variable CONN. And you said PG2.connect. You had to pass in the database name you were connecting to, pass in the user you were connecting as. The default user for any Postgres, uh, application is Postgres. And then you had to pass in the password that you set up when you're first installing PG Admin and Postgres SQL.

06:13.238 --> 06:37.943
then you had to get a cursor off of that connection. And with that cursor, you could then execute SQL queries off of it. And then once you have that cursor execute that query, you can actually fetch the rows that are returned. You can fetch many rows, you can fetch just one row, or the final option is actually to fetch all of them.

06:38.534 --> 06:50.161
And notice here, since there's quite a few rows, you may want to use this carefully. Otherwise, you'll just end up with a ton of, uh, rows returned. So let's go ahead and change that back to fetch one.

06:51.207 --> 07:18.258
So again, uh, this is really nice because you can use tuple and packing along with it. So you can say something like, um, if we just say fetch many such as 10, well, let me actually execute that query again. Notice I had to execute the query in order to, uh, fetch them off. Um, so let's go ahead and execute that query one more time. Um, and we'll say this is equal to data.

07:18.882 --> 07:43.469
And now if I take a look at data, I have this list, and I can index off of it, as you would expect, tuple unpack it, um, keep indexing off of it just to get particular data points. Uh, it's really up to you as far as what you're going to use this, uh, type of data for in this sort of Python or PsychoPG format with. But hopefully, basically, these lines are the most important ones, and they can,

07:43.469 --> 08:10.402
really be your building blocks as far as how you're going to use PostgreSQL with Python. That sort of application is really up to you. And more likely than not, it's just going to be a smaller part or back end of your application in Python when you're, uh, communicating with your database. Once you're done communicating and executing off your database, you're going to want to close your connection. So you're going to want to say your connection and go ahead and close it.

08:11.431 --> 08:29.251
And that's really all there is to it. You can check out the .ipy or Python file from the previous lecture or the IPython notebook file from the previous lecture to get a couple more useful hints and tips for how to use CyclePG2 with PostgreSQL and Python.

08:29.251 --> 08:45.738
Other than that, that's really just the basics of it. And that's kind of all you need to know to start exploring how you can use PostgreSQL with your own Python applications. Again, the most important parts are just the import, establishing that connection, creating the cursor, and then executing the SQL queries.

08:46.160 --> 09:16.012
One thing I wanted to know is to make sure that you kind of avoid using Python prints formatting or string formatting when you're executing the SQL queries. And what I mean by that is imagine you have this situation where you want to say execute and you input some commands. And let's say earlier in your code, your command line was something like, uh, maybe it was select.

09:17.328 --> 09:38.455
all from, and then you were going to add another string to it later on. So maybe that string was another object. And what I'm trying to say is you may want to be careful when you're doing this sort of thing with, uh, CyclePG and PostgreSQL with Python, because it's kind of dangerous if you accidentally call add something like

09:38.455 --> 09:58.756
drop table later on, this could kind of like potentially screw up your database. So just keep that in mind. It's always best practice to put in the entire SQL query as just one single string and to verify that it's a single string that you want. There's a couple more notes on that in the notebook, but it's just something to be aware of.

09:58.756 --> 10:22.955
Okay. With that being said, uh, that's really it as far as just, just a quick overview of how you can use CyclePG and PostgreSQL of Python. Feel free to ask any questions in the discussion section of the course. And you can also feel free to refer to the resources from the previous lecture for more hints and more tutorials on how to use this really useful library. Okay. Thanks everyone. And I'll see you at the next lecture.

