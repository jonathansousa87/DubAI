WEBVTT

00:02:17.798 --> 00:02:46.708
Então, ao mudar a direção flex para coluna, também precisamos centralizar os itens horizontalmente. Portanto, especificaremos `items-center` e adicionar algum espaçamento entre os elementos com `margin-bottom`. Abriremos um elemento h1 com as classes `text-9xl font-bold` e `mb-4`.

00:03:20.515 --> 00:03:35.724
Então, vou adicionar aqui um parágrafo com as classes `text-lg text-gray-200 mb-8`, e o conteúdo será "Desculpe, a página que você está procurando não existe". E depois precisamos.

00:00:48.560 --> 00:00:50.784
página aleatória

